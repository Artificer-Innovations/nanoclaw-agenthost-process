# Changesets

Feature PRs land on `develop` with a changeset. Cut a release by running `pnpm run version` on `develop`, open a PR to `main`, and merge — the release workflow publishes with provenance.
